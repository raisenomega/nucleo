// Item del catálogo pre-seleccionado en el ContactForm para convertir el lead en 'quote'.
export interface InterestedItem { kind: "product" | "service" | "package"; id: string; name: string; slug: string; }
